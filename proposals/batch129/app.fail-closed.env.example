# Only these non-secret overrides are proposed for the actual EnvironmentFile.
# Preserve every other setting; do not replace app.env with this delta.
SOLANA_COPY_BOT_EXECUTION_ENABLED=false
SOLANA_COPY_BOT_EXECUTION_CANARY_TINY_SUBMIT_ENABLED=false
