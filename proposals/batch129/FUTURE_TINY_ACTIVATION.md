# Отдельная будущая tiny activation

Technical fail-closed installation не даёт права на сделки. №128 закрыл один
свежий Jupiter unsigned payload/simulation criterion, source0. Не повторять
неизменённые127/128/peak audits и не менять approved protected-native policy.

До отдельного daemon-only tiny решения нужны конкретные финансовые prerequisites:

- R11: выбранная cohort/leaders, срок и разрешённый фактический risk budget,
  наблюдаемая route/SELL/coverage, opening wallet/ownership и критерий остановки;
  synthetic B/F128 и публичный payer не подтверждают право распоряжаться wallet.
- Execution bridge: durable_association_v1 пока требует оба execution flags=false
  в `crates/config/src/association_delivery.rs`. Одного activate=true недостаточно;
  нельзя переключить legacy/durable режим, чтобы обойти этот blocker.
- Настоящие signer/wallet authority, pending/OPEN/Unknown reconciliation, согласованные
  admission/funding/priority/fee/loss limits, canonical BUY/SELL receipt/ownership и
  сохранность учёта при остановке; existing experiment ID/state не rearm/reset.
- Exact app CI artifact/config/migration/rollback и bounded live technical proof,
  затем отдельное разрешение measured tiny cohort через daemon с финансовым
  reconciliation и критерием net после издержек. Landing/exit/profit неизвестны.

№126/R1 budget и policy127 уже приняты в локальном scope, их пересмотр не часть129.
До live cohort остаются deployment/execution authority и реальные inputs; эти
Unknown не дефекты127 и не причина назначать ещё один одинаковый source audit.
Sign/send/funding/manual trades0; no automatic repair/retry/rearm при первом RED.
