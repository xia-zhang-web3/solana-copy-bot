use super::*;

#[derive(Debug, Clone, Serialize)]
pub(super) struct SnapshotOutput {
    pub(super) event: String,
    pub(super) state: String,
    pub(super) latest_surface_status: String,
    pub(super) latest_surface_action: String,
    pub(super) config_path: String,
    pub(super) source_db_path: String,
    pub(super) snapshot_path: String,
    pub(super) metadata_path: String,
    pub(super) archive_path: Option<String>,
    pub(super) staged_snapshot_path: Option<String>,
    pub(super) staged_metadata_path: Option<String>,
    pub(super) cadence_minutes: Option<u64>,
    pub(super) retention: Option<usize>,
    pub(super) pruned_snapshot_paths: Vec<String>,
    pub(super) cleanup_removed_paths: Vec<String>,
    pub(super) archive_promoted: bool,
    pub(super) archive_set_count_before: Option<usize>,
    pub(super) archive_set_count_after: Option<usize>,
    pub(super) staged_progress_resumed: bool,
    pub(super) staged_seeded_from_latest_surface: bool,
    pub(super) staged_progress_preserved_for_retry: bool,
    pub(super) staged_progress_advanced: bool,
    pub(super) staged_completed_batches: usize,
    pub(super) staged_source_rows_loaded: usize,
    pub(super) staged_rows_processed: usize,
    pub(super) staged_rows_inserted: usize,
    pub(super) staged_row_count_before_attempt: Option<usize>,
    pub(super) staged_row_count_after_attempt: Option<usize>,
    pub(super) staged_covered_through_cursor_before_attempt: Option<DiscoveryRuntimeCursor>,
    pub(super) staged_covered_through_cursor_after_attempt: Option<DiscoveryRuntimeCursor>,
    pub(super) staged_terminal_phase: Option<String>,
    pub(super) staged_source_read_duration_ms: u64,
    pub(super) staged_write_duration_ms: u64,
    pub(super) staged_write_rows_per_second: Option<f64>,
    pub(super) staged_write_batch_count: usize,
    pub(super) staged_write_batch_rows: usize,
    pub(super) staged_write_sqlite_variable_limit: usize,
    pub(super) staged_write_statement_params_per_row: usize,
    pub(super) staged_write_statement_chunk_row_cap: usize,
    pub(super) staged_write_effective_statement_chunk_rows: usize,
    pub(super) staged_write_statement_count: usize,
    pub(super) staged_write_rows_processed: usize,
    pub(super) staged_write_rows_inserted: usize,
    pub(super) staged_write_value_build_duration_ms: u64,
    pub(super) staged_write_prepare_duration_ms: u64,
    pub(super) staged_write_execute_duration_ms: u64,
    pub(super) staged_write_state_refresh_duration_ms: u64,
    pub(super) staged_write_state_upsert_duration_ms: u64,
    pub(super) staged_write_transaction_duration_ms: u64,
    pub(super) staged_write_deadline_exhausted_before_statement: bool,
    pub(super) staged_write_deadline_exhausted_during_execute: bool,
    pub(super) source_db_bytes: u64,
    pub(super) source_wal_bytes: u64,
    pub(super) source_total_bytes: u64,
    pub(super) source_page_size_bytes: usize,
    pub(super) source_page_count: usize,
    pub(super) snapshot_pages_per_step: i32,
    pub(super) snapshot_pause_between_steps_ms: u64,
    pub(super) snapshot_max_attempt_duration_ms: Option<u64>,
    pub(super) attempt_duration_ms: u64,
    pub(super) backup_step_count: usize,
    pub(super) backup_retry_count: usize,
    pub(super) busy_retry_count: usize,
    pub(super) locked_retry_count: usize,
    pub(super) backup_total_page_count: Option<usize>,
    pub(super) backup_remaining_page_count: Option<usize>,
    pub(super) backup_copied_page_count: Option<usize>,
    pub(super) terminal_reason: Option<String>,
    pub(super) retryable_reason: Option<String>,
    pub(super) deferred_reason: Option<String>,
    pub(super) hard_failure_reason: Option<String>,
    pub(super) created_at: Option<DateTime<Utc>>,
    pub(super) row_count: Option<usize>,
    pub(super) covered_since: Option<DateTime<Utc>>,
    pub(super) covered_through_cursor: Option<DiscoveryRuntimeCursor>,
    pub(super) last_batch_completed_at: Option<DateTime<Utc>>,
    pub(super) snapshot_bytes: Option<u64>,
}
